# implement function with argument length parameter
def argsLengthFunction(*args):
    total = sum(args)
    if not args:
        return 2
    else:
        return total

res = argsLengthFunction(4,100,23)
print(res)
