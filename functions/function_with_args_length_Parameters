# implement function with argument length parameter
def argsLengthFunction(*args):
    
    if any(arg < 0 for arg in args):
        return "Negative number argument not allowed"
    
    if not args:
        return 2
    else:
        return sum(args)

res = argsLengthFunction()

print(res)
